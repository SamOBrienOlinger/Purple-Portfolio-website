// iAIteractivity or timing logic
document.addEventListener('DOMContentLoaded', () => {
    const whale = document.querySelector('.whale');
    
    // Delay animation or sync with other elements
    setTimeout(() => {
        whale.style.animationPlayState = 'running';
    }, 1000); // Delay animation start by 1 second
});

